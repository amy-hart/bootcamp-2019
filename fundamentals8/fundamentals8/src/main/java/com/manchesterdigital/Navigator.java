package com.manchesterdigital;

/*
public class Navigator {

    private String direction;
    private Integer currentPosition = 0;

    public Navigator(String direction) {
        this.direction = direction;
    }

    private static void changePosition(Integer currentPosition, Integer degrees) {
        Integer newPosition = currentPosition += degrees;

        System.out.println("New position: " + newPosition);

        checkNewDirection(newPosition);
    }

    private static CompassDirections checkNewDirection(Integer newPosition) {

        CompassDirections subject = new CompassDirections();

        return subject.getDirectionByDegrees(newPosition);

    }

    public static void main(String[] args) {
        changePosition(90,45);
    }
}
*/