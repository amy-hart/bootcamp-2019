package com.manchesterdigital;

public class SandBox {
    /*
    // Convert array into a string
    String [] stringNumbersArray = stringNumbers.split(",");
    // Create new int array with the same length as the string array
    //int [] intArray = new int[stringNumbersArray.length];

    // Loop through the intArray and grab converted numbers from the string array
    int sumOfArray = 0;
        for (int i = 0; i < stringNumbersArray.length; i ++) {
        sumOfArray += Integer.parseInt(stringNumbersArray[i]);
    }
    */

}
