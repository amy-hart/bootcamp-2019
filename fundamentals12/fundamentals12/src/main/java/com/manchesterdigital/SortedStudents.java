package com.manchesterdigital;

/*
import java.util.List;
import java.util.function.Predicate;
import java.util.stream.Collectors;

public class SortedStudents {
    public static void main(String[] args) {
        // My attempt

        // English
        Student student1 = new Student("Amy", "English", 70);
        Student student2 = new Student("Sam", "English", 50);
        Student student3 = new Student("Jill", "English", 30);
        // German
        Student student4 = new Student("Andrew", "German", 75);
        Student student5 = new Student("Samantha", "German", 57);
        Student student6 = new Student("John", "German", 20);
        // French
        Student student7 = new Student("Alex", "French", 80);
        Student student8 = new Student("Stanley", "French", 49);
        Student student9 = new Student("Bob", "French", 40);

        List<Student> students = List.of(student1, student2, student3, student4, student5,
                student6, student7, student8, student9);

        students.stream()
                .filter(student -> student.getSubject().equalsIgnoreCase("English"))
                .filter(student -> student.getScore() > 50)
                .filter(student -> student.getName().startsWith("A") || student.getName().startsWith("S"))
                .forEach(student -> System.out.println("student = " + student.toString()));

       students.stream()
                .filter(student -> student.getSubject().equalsIgnoreCase("German"))
                .filter(student -> student.getScore() > 50)
                .filter(student -> student.getName().startsWith("A") || student.getName().startsWith("S"))
                .forEach(student -> System.out.println("student = " + student.toString()));

        students.stream()
                .filter(student -> student.getSubject().equalsIgnoreCase("French"))
                .filter(student -> student.getScore() > 50)
                .filter(student -> student.getName().startsWith("A") || student.getName().startsWith("S"))
                .forEach(student -> System.out.println("student = " + student.toString()));
    }
    */

